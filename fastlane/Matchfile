git_url ENV["MATCH_GIT_URL"]   # SSH URL, e.g. git@github.com:Wrexist/ios-certificates.git
                               # SSH auth is handled by the webfactory/ssh-agent workflow step
storage_mode "git"
type "appstore"
app_identifier ["com.wrexist.silicon"]
